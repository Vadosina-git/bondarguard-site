# Placeholder-ресурсы

Все ниже — временные материалы. Заменить перед запуском в прод.

## Команда (фотографии)
- `public/images/team/*` — 4 портрета. Сейчас нет файлов; в `team.json` проставлен флаг `placeholder: true`. Рекомендация: реальные фото сотрудников 400×400 WebP, выровненные по композиции.

## Отзывы
- `src/content/reviews/reviews.json` — все отзывы помечены `placeholder: true`. Тексты написаны по реалистичным паттернам. Заменить на реальные отзывы клиентов (можно попросить оставить в Яндекс.Картах и импортировать).

## Реквизиты
- `src/data/site.ts` — `inn`, `ogrn` — заглушки.
- `phone`, `phoneTel`, `email`, `address` — заглушки.

## Цены
- `priceKvartiraFrom`, `priceDomFrom`, `priceBiznesFrom` — заглушки.
- Время реагирования ГБР — заглушки.

## Лицензии / сертификаты / партнёры
- Нигде не показываются реальные номера. Где упоминается — «Документы на согласовании». Заменить на реальные сканы / номера.

## Политика конфиденциальности
- `src/pages/policy.astro` — базовый шаблон под 152-ФЗ. ПРОВЕРИТЬ ЮРИСТОМ перед публикацией.

## Hero / фоновые изображения
- Секции используют абстрактные SVG-композиции — замены не требуют. Если захочется фото — подобрать с Unsplash по темам «modern cottage night», «security camera» и класть в `public/images/hero/`.

## OG image
- `public/images/og/default.jpg` сгенерирован из mark + название. Для отдельных страниц можно генерировать персональные.

## Яндекс.Метрика
- `src/data/site.ts` → `analytics.yandexMetrikaId` — пусто. Вставить реальный counter ID перед запуском.
